# Days 1–7 (Python fundamentals) run on the standard library alone — no installs needed.
#
# The AI track (Day 8+) adds dependencies here. Install with:
#   pip install -r requirements.txt

# --- Phase 1 · Day 8 — Python for AI ---
requests                   # call web APIs (and LLMs)
httpx                      # async HTTP client (async equivalent of requests)
pydantic                   # data validation / structured output models
# python-dotenv listed below (shared with Day 16)

# --- Phase 1 · Day 12 — Multi-provider ---
groq                       # fast hosted open models (free tier) — needs GROQ_API_KEY
ollama                     # run open models locally (no key) — needs the Ollama app

# --- Phase 1 · Day 13 — Working with data ---
pdfplumber                 # extract text from PDFs
beautifulsoup4             # parse HTML for light web scraping
# (json + csv are in the standard library)

# --- Phase 2 · Day 16 — Embeddings (RAG & Memory) ---
numpy
sentence-transformers      # local, free embeddings (all-MiniLM-L6-v2)
google-generativeai        # Gemini embeddings (free tier) — needs GEMINI_API_KEY
python-dotenv              # load GEMINI_API_KEY from a .env file
